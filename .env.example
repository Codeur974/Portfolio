# Configuration SMTP pour l'envoi d'emails
# Ces variables doivent être configurées dans Netlify > Site settings > Environment variables

# Serveur SMTP (ex: smtp.gmail.com, smtp-mail.outlook.com)
SMTP_HOST=smtp.gmail.com

# Port SMTP (587 pour TLS, 465 pour SSL)
SMTP_PORT=587

# Email utilisé pour l'envoi
SMTP_USER=votre-email@gmail.com

# Mot de passe ou App Password
SMTP_PASS=votre-mot-de-passe-app

# Email expéditeur (optionnel, utilise SMTP_USER par défaut)
SMTP_FROM=votre-email@gmail.com

# Email administrateur qui recevra les notifications d'avis
ADMIN_EMAIL=votre-email@gmail.com
